# Copy to .env and fill in. NEVER commit .env — it is gitignored, and CI runs gitleaks.
# Inspect reads provider keys from the environment.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Add other provider keys only as evaluations require them.
