# smoke

End-to-end plumbing check for the Inspect harness: one sample, one `generate()` solver,
exact-match scorer.

**What it measures:** that the harness, provider credentials and `.eval` logging work.

**What it does NOT measure:** anything about model capability, safety, robustness or
fitness for purpose. Never cite this task in a report.
